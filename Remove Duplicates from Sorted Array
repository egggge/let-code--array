在一个含有重复数字的数组中计算出互异数字的个数。
Given nums = [0,0,1,1,1,2,2,3,3,4] return 5.
class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
#遇到数组首先应该考虑异常情况
        if(nums.size()==0) return 0;
        int j=0;
#for循环中三个顺序不能随意调换
        for(int i=1;i<nums.size();i++){
            if(nums[i]!=nums[j]){
                j++;
                nums[j]=nums[i];
            }    
        }
        return j+1;
    }
};
